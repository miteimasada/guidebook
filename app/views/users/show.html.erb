<div class = 'wrapper'>
  <div class = 'container'>
    <div class = 'content'>
      <div class = 'show-title'>
        <h1><%= @user.name %></h1>
        <p>登録日：<%= @user.created_at.strftime('%Y/%m/%d') %></p>
      </div>
      <div class = 'show-text'>
        <p><%= @user.profile %></p>
      </div>
      <div class = 'show-menu'>
        <%= link_to("編集", "/users/#{@user.id}/edit") %>
      </div>
    </div>
  </div>
</div>
