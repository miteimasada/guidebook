<div class = 'wrapper'>
  <div class = 'container'>
    <div class = 'content'>

      <% @user.errors.full_messages.each do |message| %>
        <div class="form-error">
          <%= message %>
        </div>
      <% end %>

      <%= form_tag('/users/create') do %>
        <div class = 'form'>
            <h1>新規登録</h1>
            <p class = form-info>ユーザー登録をすると、記事の投稿やお気に入り登録等ができます。</p>
            <div class = 'form-title'>
              <p>ユーザー名</p>
              <input name="name" value="<%= @user.name %>">
            </div>
            <div class = 'form-content'>
              <p>メールアドレス</p>
              <input name="email" value="<%= @user.email %>">
              <p>パスワード</p>
              <input type="password" name="password" value="<%= @user.password %>">
            </div>
            <p class = 'form-alert'>
            ※メールアドレスとパスワードはログイン時に必要となります。<br>
            ※パスワードの登録後の変更は出来ません。
            </p>
            <div class = 'btn'>
              <input type="submit" value="新規登録">
            </div>
        </div>
      <%end%>
    </div>
  </div>
</div>
