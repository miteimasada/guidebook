<div class= 'home-title'>
  <h1>One for all.<br>All for one.</h1>
  <p>TORISETSU は、あなたの初めてを記録・共有する為のサービスです。<br>初めて行くお店。初めて使うサービス。あらゆる初めてを共有し、誰かの初めての手助けをしましょう。</p>
  <%# <h1>ここに一番伝えたいことを書く</h1>
  <p>ここにはさーびすの簡単な説明を書く。<br>なるべく簡潔にどんなサービスを提供するのかを説明する。</p> %>
</div>

<div class = 'home-index-wrapper'>

  <div class = 'home-index-container'>

    <div class = 'home-index'>
      <p class = 'home-index-title'>新着記事</p>

      <% @posts.each do |post|%>
        <%@likes_count = Like.where(post_id: post.id).count%>
        <div class = 'home-index-list'>

          <div class = 'home-index-left'>
            <div class = 'home-index-img'>
              <%= link_to(image_tag("/user_images/#{post.user.image_name}"), "/users/#{post.user.id}") %>
            </div>

            <div class = 'home-index-text'>
              <%= link_to( post.title,"/posts/#{post.id}") %>
              <p><%= post.content %></p>
            </div>
          </div>

          <div class = 'created_at'>
            更新日：<%= post.created_at.strftime('%Y/%m/%d') %>
            <div class = 'home-likes'>
              <span class="fa fa-heart like-btn-unlike"></span>
              <%= @likes_count %>
            </div>
          </div>

        </div>
      <%end%>
    </div>

    <div class = 'likes-index'>
      <p>TOP5</p>
      <% @all_ranks.each do |post|%>
        <%@likes_count = Like.where(post_id: post.id).count%>
        <div class = 'home-index-list'>

          <div class = 'home-index-left'>
            <div class = 'home-index-img'>
              <%= link_to(image_tag("/user_images/#{post.user.image_name}"), "/users/#{post.user.id}") %>
            </div>

            <div class = 'home-index-text'>
              <%= link_to( post.title,"/posts/#{post.id}") %>
              <p><%= post.content %></p>
            </div>
          </div>

          <div class = 'created_at'>
            更新日：<%= post.created_at.strftime('%Y/%m/%d') %>
            <div class = 'home-likes'>
              <span class="fa fa-heart like-btn-unlike"></span>
              <%= @likes_count %>
            </div>
          </div>

        </div>
      <%end%>


    </div>

  </div>

</div>
